# Paper Record Schema

Each candidate paper is represented as a `PaperRecord` with stable identifiers, bibliographic metadata, seed relations, open-access status, download status, ranking scores, a human-readable ranking reason, and provenance.

Do not invent missing DOI, arXiv ID, or author identifiers. Mark ambiguous matches with `needs_review: true`.

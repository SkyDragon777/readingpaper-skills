# Synthesis Contract

`synopticpaper` reads existing `outputs/finalpaper/` and `outputs/relevantpaper/` artifacts and writes `outputs/synopticpaper/` artifacts. It must not perform external search or download PDFs by default.
